runid = mrunid

failfast = false

directory{ 
	project {
		tests = "./tests"
		root = "./"${project.name}
	}
}

session {
	name = msession
}

report {
	mode = minimal
		
	generators {
		builtin = [excel]
	}
	
	listeners {
		builtin = [console]
	}
}

project {
	name = mproject
}