request:
  method: post
  route: "http://httpbin.org/post"
  content_type: json
  content: "null"

response:

  expected:
  
    json:
      jpaths: 
        json: null # httpbin returns null object not null string as root object is null
