labels:

  menu:
    id: adminmenu

  settings:
    link: Settings

  settings_max_wait:
    link: Settings
    max_wait: 10

  first_div:
    xpath: "//div"

  non_existing:
    id: absent

  non_existing_max_wait:
    id: absent
    max_wait: 10